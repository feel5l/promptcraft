/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

export interface ErrorResponse {
  error: string;
  details?: string;
}

export interface ModelInfo {
  id: string;
  name: string;
}

export interface ProviderInfo {
  id: string;
  name: string;
  keyPrefix: string;
  models: ModelInfo[];
}

export interface ToolInfo {
  id: string;
  name: string;
  description: string;
}

export interface TechniqueInfo {
  id: string;
  name: string;
  description: string;
}

export interface ProvidersResponse {
  providers: ProviderInfo[];
  imageTools: ToolInfo[];
  videoTools: ToolInfo[];
  textTechniques: TechniqueInfo[];
}

/**
 * Prompt engineering technique to apply
 */
export type TextPromptRequestTechnique =
  (typeof TextPromptRequestTechnique)[keyof typeof TextPromptRequestTechnique];

export const TextPromptRequestTechnique = {
  "zero-shot": "zero-shot",
  "few-shot": "few-shot",
  "chain-of-thought": "chain-of-thought",
  role: "role",
  "xml-tags": "xml-tags",
  auto: "auto",
} as const;

export interface TextPromptRequest {
  /** Description of the task or goal */
  task: string;
  /** Domain or field (e.g., coding, creative writing, data analysis) */
  domain: string;
  /** Prompt engineering technique to apply */
  technique: TextPromptRequestTechnique;
  /** AI provider id (openai, anthropic, gemini, openrouter) */
  provider: string;
  /** Model id to use */
  model: string;
  /** API key for the provider */
  apiKey: string;
  /** Preferred output language (ar, en, auto) */
  language?: string;
}

/**
 * The image generation tool to optimize for
 */
export type ImagePromptRequestTargetTool =
  (typeof ImagePromptRequestTargetTool)[keyof typeof ImagePromptRequestTargetTool];

export const ImagePromptRequestTargetTool = {
  midjourney: "midjourney",
  dalle3: "dalle3",
  "stable-diffusion": "stable-diffusion",
  flux: "flux",
  ideogram: "ideogram",
  firefly: "firefly",
} as const;

export interface ImagePromptRequest {
  /** Simple description of the desired image */
  description: string;
  /** The image generation tool to optimize for */
  targetTool: ImagePromptRequestTargetTool;
  /** Optional style hint (e.g., photorealistic, anime, oil painting) */
  style?: string;
  /** Desired aspect ratio (e.g., 16:9, 1:1, 9:16) */
  aspectRatio?: string;
  provider: string;
  model: string;
  apiKey: string;
  language?: string;
}

/**
 * The video generation tool to optimize for
 */
export type VideoPromptRequestTargetTool =
  (typeof VideoPromptRequestTargetTool)[keyof typeof VideoPromptRequestTargetTool];

export const VideoPromptRequestTargetTool = {
  sora: "sora",
  runway: "runway",
  kling: "kling",
  pika: "pika",
  luma: "luma",
  hailuo: "hailuo",
} as const;

export interface VideoPromptRequest {
  /** Description of the scene or concept */
  description: string;
  /** The video generation tool to optimize for */
  targetTool: VideoPromptRequestTargetTool;
  /** Desired duration hint (e.g., 5s, 10s, 30s) */
  duration?: string;
  /** Camera movement preference (e.g., cinematic, handheld, drone) */
  cameraStyle?: string;
  provider: string;
  model: string;
  apiKey: string;
  language?: string;
}

export interface PromptSection {
  label: string;
  content: string;
  purpose: string;
}

export interface QualityScore {
  /**
   * @minimum 1
   * @maximum 10
   */
  clarity: number;
  /**
   * @minimum 1
   * @maximum 10
   */
  detail: number;
  /**
   * @minimum 1
   * @maximum 10
   */
  actionability: number;
  /**
   * @minimum 1
   * @maximum 10
   */
  overall: number;
  feedback: string;
}

export interface PromptResponse {
  /** The generated prompt */
  prompt: string;
  /** Breakdown of prompt sections */
  explanation: PromptSection[];
  qualityScore: QualityScore;
  technique: string;
  tokensUsed?: number;
}

/**
 * Tool-specific parameters (--ar, --style, etc.)
 */
export type ImagePromptResponseParameters = { [key: string]: string };

export interface ImagePromptResponse {
  /** The main optimized prompt */
  prompt: string;
  /** Negative prompt (for SD/Flux) */
  negativePrompt?: string;
  /** Tool-specific parameters (--ar, --style, etc.) */
  parameters?: ImagePromptResponseParameters;
  /** Usage tips for the specific tool */
  tips: string[];
  tokensUsed?: number;
}

export interface VideoPromptResponse {
  /** The main video prompt */
  prompt: string;
  /** Camera movement and shot type directions */
  cameraDirections: string;
  /** Technical specifications (duration, fps, style) */
  technicalSpec: string;
  /** Tool-specific tips and limitations to avoid */
  tips: string[];
  tokensUsed?: number;
}

export interface OptimizeRequest {
  /** The existing prompt to be improved */
  originalPrompt: string;
  /** What the user wants to improve or what is wrong with the prompt */
  improvementGoal: string;
  /** Optional additional context about the use case */
  context?: string;
  provider: string;
  model: string;
  apiKey: string;
  language?: string;
}

/**
 * The type of change made
 */
export type PromptChangeType =
  (typeof PromptChangeType)[keyof typeof PromptChangeType];

export const PromptChangeType = {
  addition: "addition",
  removal: "removal",
  modification: "modification",
  restructure: "restructure",
} as const;

export interface PromptChange {
  /** The type of change made */
  type: PromptChangeType;
  /** Human-readable description of what changed and why */
  description: string;
}

export interface OptimizeResult {
  /** The improved version of the prompt */
  improvedPrompt: string;
  /** List of changes made to the prompt */
  changes: PromptChange[];
  beforeScore: QualityScore;
  afterScore: QualityScore;
  tokensUsed?: number;
}
